# Корочки.есть - Backend API

Backend для портала онлайн курсов дополнительного профессионального образования.

## 🚀 Быстрый запуск

```bash
# 1. Настройте DATABASE_URL в backend/.env
# 2. Установите зависимости и настройте БД
cd backend
npm install
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed

# 3. Запустите
npm run start:dev
```

**API:** http://localhost:3000  
**Swagger:** http://localhost:3000/api/docs

## 🔑 Учетные данные

**Администратор:** Login: `Admin`, Password: `KorokNET`

## 📋 API Endpoints

### Авторизация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход

### Пользователи
- `GET /users/profile` - Свой профиль
- `GET /users` - Все пользователи

### Курсы
- `GET /courses` - Все курсы (доступно всем)
- `GET /courses/:id` - Курс по ID
- `POST /courses` - Создать курс (только админ)
- `PATCH /courses/:id` - Изменить курс (только админ)
- `DELETE /courses/:id` - Удалить курс (только админ)

### Заявки на курсы
- `POST /applications` - Создать заявку (указать название курса)
- `GET /applications/my` - Свои заявки
- `GET /applications` - Все заявки (только админ)
- `PATCH /applications/:id/status` - Изменить статус (только админ)

### Отзывы
- `POST /reviews` - Создать отзыв
- `GET /reviews` - Все отзывы
- `PATCH /reviews/:id` - Изменить отзыв

## 🗄 База данных

- **users** - пользователи
- **courses** - курсы (название, учитель, описание, изображение)
- **applications** - заявки (связь с курсом)
- **reviews** - отзывы

## 🎯 Как работать с курсами

**Просмотр курсов (любой пользователь):**
```
GET /courses
```

**Создание курса (только админ):**
```json
POST /courses
{
  "name": "Новый курс",
  "teacher": "Преподаватель",
  "description": "Описание курса",
  "image": "https://example.com/image.jpg"
}
```

**Создание заявки:**
```json
POST /applications
{
  "courseName": "Основы программирования на Python",
  "startDate": "2024-03-01",
  "paymentMethod": "CASH"
}
```

Система автоматически найдет курс по названию.

